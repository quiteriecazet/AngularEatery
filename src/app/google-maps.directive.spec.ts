import { GoogleMapsDirective } from './google-maps.directive';

describe('GoogleMapsDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleMapsDirective(this);
    expect(directive).toBeTruthy();
  });
});
